version: v1
name: projectname

contract:
  address: '0x'
  path: ./contracts/example.sol
  name: example
  compilerVersion: 0.5.8

blockchain:
  platform: ethereum
  network: mainnet

handlers:
  event1:
    event: Execution(address)
    # If you want to include additional logic, you need to uncomment it.
    # version: v1
    # runtime: tsnode8
    # path: ./handlers/execution.ts
    # dep: ./handlers/package.json
    # function: execution

  event2:
    event: Execution(address,address)
    # If you want to include additional logic, you need to uncomment it.
    # version: v1
    # runtime: tsnode8
    # path: ./handlers/execution2.ts
    # dep: ./handlers/package.json
    # function: execution2

webhook:
  url: https://localhost:8080
  method: POST
  headers: 
    Authorization: 'Bearer YOUR-OWN-TOKEN'
